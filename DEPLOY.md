# النشر على الإنتاج

خطوات مرتّبة. نفّذها بالترتيب — كل خطوة تفترض أن الّتي قبلها تمّت.

> **قاعدة واحدة تحكم كل ما تحت:** لا تنسخ سرّاً إلى محادثة ولا إلى ملف
> داخل المستودع. الأسرار مكانها متغيّرات البيئة على الخادم وحدها، ومن
> يراها هو أنت.

---

## ١. متغيّرات البيئة

هذه الثلاثة **لازمة**، ويرفض النظام العمل بدونها في الإنتاج:

| المتغيّر | ما هو | إن غاب |
|---|---|---|
| `DATABASE_URL` | وصلة PostgreSQL | لا يقلع الخادم |
| `JWT_SECRET` | توقيع الجلسات — ٣٢ حرفاً فأكثر | **يرفض الإقلاع** بدل أن يستعمل مفتاح التطوير |
| `APP_ENCRYPTION_KEY` | تشفير أسرار شركات الشحن والتطبيقات | لا تُحفظ كلمة مرور شركة شحن إطلاقاً — يردّ بخطأ بدل أن يخزّنها مكشوفة |

توليد مفتاحين جديدين (٦٤ خانة hex = ٣٢ بايت حقيقية):

```bash
node -e "console.log('JWT_SECRET=' + require('crypto').randomBytes(32).toString('hex'))"
```

```bash
node -e "console.log('APP_ENCRYPTION_KEY=' + require('crypto').randomBytes(32).toString('hex'))"
```

**تبديل `APP_ENCRYPTION_KEY` بعد أن تُحفظ أسرار به يجعلها غير قابلة
للفكّ.** ولّده مرّة، واحتفظ به حيث تحتفظ بكلمات مرورك.

اختيارية: `APP_URL` و`APP_DOMAIN` (للنطاقات الخاصة)،
`OPENROUTER_API_KEY` (الإدخال بالذكاء الاصطناعي)، ومجموعات
`TELEGRAM_*` و`WHATSAPP_*` لمن يستعملها.

---

## ٢. قبل الترحيل — تحقّق لا يغيّر شيئاً

ترحيل `20260920160000_drop_crm` يحذف جداول CRM. شغّل الفحص أولاً:

```bash
psql "$DATABASE_URL" -f prisma/migrations/20260920160000_drop_crm/preflight.sql
```

**كل صفّ يجب أن يقرأ صفراً.** إن لم يقرأ، صدّر ذلك الجدول قبل أي شيء —
الترحيل سيرفض العمل أصلاً، لكنك تريد أن تعرف الآن لا في منتصف نشر.

خذ نسخة احتياطية قبل الترحيل، دائماً:

```bash
pg_dump "$DATABASE_URL" -Fc -f backup-before-deploy.dump
```

---

## ٣. الترحيل

```bash
npx prisma migrate deploy
```

ثم تأكّد:

```bash
npx prisma migrate status
```

يجب أن يقول `Database schema is up to date!`.

كل ترحيل في هذا المستودع معه `rollback.sql` بجانبه. للتراجع عن واحد:

```bash
psql "$DATABASE_URL" -f prisma/migrations/<اسم الترحيل>/rollback.sql
```

---

## ٤. البناء والتشغيل

```bash
npm ci && npx prisma generate && npm run build
```

> على ويندوز: **أوقف خادم التطوير قبل `prisma generate`** — وإلا يفشل
> بصمت لأن الملف مقفل، ويبقى العميل القديم يعطي أخطاء «العمود غير موجود».

---

## ٥. ما يُدخَل من داخل التطبيق، لا من ملفّ

| الشيء | المكان | ملاحظة |
|---|---|---|
| بيانات شركة الشحن (LogesTechs) | `/settings/couriers` ← «الاعتماديات» | كلمة المرور **تُكتب ولا تُقرأ**: لا endpoint يعيدها ولا سجلّ تدقيق يحفظها. تُشفَّر قبل الحفظ، وإن غاب مفتاح التشفير يرفض الحفظ. |
| رقم الدعم في واجهة المتجر | إعدادات المتجر | الواجهة العامة منشورة ويقرأها الزبائن. |
| كلف التشغيلات | `/inventory/manufacturing` ← «عدّل الكلفة» | بدونها «صافي الربح» هو ربح إجمالي. تسري على المخزون المتبقي وعلى الطلبات الجديدة — لا على المكتوبة سابقاً. |
| ساعات الدوام والعطلة | `/settings/geo` لكل بلد | منها تُحسب كل أزمنة تقرير الأداء والتأخير. |

---

## ٦. بعد النشر — تحقّق سريع

1. سجّل دخولاً واحداً، وافتح `/growth/performance` — يجب أن تظهر أرقام لا أصفار.
2. افتح واجهة المتجر العامة وتأكّد أنها المتجر الذي تقصده وأن رقم الدعم رقمك.
3. أدخل اعتماديات شركة الشحن، ثم رحّل دفعة واحدة وتأكّد أن الباركود رجع.
4. راجع `/settings/system` — أي تكامل تريده مطفأ يجب أن يكون مطفأ.
