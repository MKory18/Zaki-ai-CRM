# مهام مؤجلة

مهام اتفقنا على تأجيلها. **لا تُنفَّذ إلا بطلب صريح** — يكفي أن تقول
«نفّذ رقم ٣» أو «نفّذ ١ و٤».

الترقيم ثابت: المهمة المنجزة تُشطب ويبقى رقمها، فلا ينزلق ما بعدها.

---

## ١. الإيموجي في أزرار التأكيد

أزرار نتيجة الاتصال تستعمل 📞 ☎️ ⏰ 🔄 ✅ ❌ 🚫 — لغة بصرية لا توجد في أي
شاشة أخرى من النظام. تُستبدل بأيقونات وألوان الواجهة نفسها.

**المكان:** `src/components/orders/ConfirmationActions.tsx`
**أُجّلت بطلبك:** إلى ما بعد اكتمال الموقع.

---

## ~~٢. تقارير المنتجات على طلب متعدد المنتجات~~ — أُنجزت

التقارير تُجمّع حسب `orders.productId`، وهو **المنتج الأول في الطلب**. طلب
فيه منتجان يُحسب كله على أوّلهما في تقرير «أفضل المنتجات».

الأرقام المالية صحيحة (كلها من `totalAmount`)؛ الخلل في التوزيع على
المنتجات وحده. العلاج: التقرير يقرأ `order_items` بدل عمود المنتج.

**المكان:** `src/lib/analytics.ts` — `productGroups`
**يظهر أثره:** فقط حين تبدأ ببيع طلبات متعددة المنتجات فعلاً.

---

## ~~٣. توحيد تخزين أرقام الهواتف~~ — أُنجزت

العملاء تُخزَّن أرقامهم بـ `normalizePhoneNumber` (مكتوبة لمصر: تُعالج
بادئة ٢٠ وحدها)، بينما القائمة السوداء تطابق بـ `canonicalPhone` (تعرف
سوريا والأردن والسعودية والإمارات والعراق).

الأثر: رقم سوري مكتوب دولياً قد يُخزَّن `963932374769` ولا يطابق العميل
نفسه المخزَّن `0932374769` — فيُنشأ عميل مكرر، أو يفلت رقم محظور.

العلاج: ترحيل يوحّد `customers.phone` على `canonicalPhone`، مع تقرير قبل
التنفيذ بما سيتغيّر. **١٥٧ عميلاً** في البيانات الحالية.

**المكان:** `src/lib/phone.ts` · `src/lib/phone-rules.ts`
**يحتاج:** ترحيلاً على بيانات حيّة + `rollback.sql`.

---

## ~~٤. تاريخ إنشاء الطلبات المستوردة~~ — أُنجزت يدوياً (كلها ١٠ أيلول)

الطلبات المستوردة من كشف باشا أخذت `createdAt` = لحظة الاستيراد، بينما
`shippedAt` جاء من الكشف. النتيجة أن بطاقة «الإدخال» تظهر بتاريخ بعد
«الشحن» — ٢٠ سبتمبر مقابل ١٥ سبتمبر.

العلاج: الاستيراد يقرأ تاريخ الطلب من الكشف حين يكون فيه، أو ترحيل يصحّح
الموجود من `shippedAt`.

**المكان:** `src/lib/settlement.ts` — `parseStatement`

---

## ~~٥. عهدة المندوب~~ — أُنجزت (شاشة /finance/agents)

المندوب يحمل بضاعة ومالاً بين استلامه الشحنة وتسليمه التحصيل. لا يوجد
اليوم كشف يقول: ماذا بيده الآن، وكم علينا له أو له علينا.

**من العقد:** المرحلة العاشرة.

---

## ٦. الحملات الإعلانية

شاشة `/growth/campaigns` معلَّمة بالمرحلة ٨ وتعرض «قيد الإنشاء». تحتاج
بيانات دخول منصات الإعلان (فيسبوك/تيكتوك) لتقرأ الإنفاق وتربطه بالطلبات.

**تحتاج منك:** صلاحيات الوصول لحسابات الإعلان.

---

## ~~٧. متجر المنتج الواحد ومتجر التطبيقات~~ — أُنجزت

`/growth/single-product-stores` و`/apps/store` و`/apps/installed` مبنية
وشغّالة، ولم تعد أي شاشة تعرض «قيد الإنشاء».

---

## قرارات تنتظرك (ليست مهامّ برمجية)

- **بيانات LogesTechs** (بريد وكلمة مرور) — بدونها تبقى «باشا» يدوية رغم
  ربطها بـ `companyId 744`.
- **سؤال LogesTechs عن الـ webhooks** — اليوم نسحب الحالات بالاستطلاع.
- **النشر على الإنتاج** — الخطوات مكتوبة بالترتيب في `DEPLOY.md`: مفتاحا
  `JWT_SECRET` و`APP_ENCRYPTION_KEY`، ثم `preflight.sql`، ثم
  `prisma migrate deploy`.
- **كلف التشغيلات** — ٨٩ تشغيلة بكلفة صفر، منها ٥ فيها كل ما بيع. تُدخَل من
  `/inventory/manufacturing` ← «عدّل الكلفة». بدونها «صافي الربح» ربح إجمالي.
  تسري على المخزون المتبقي والطلبات الجديدة — **لا** على الطلبات المكتوبة
  سابقاً، لأن كلفة الطلب تُلتقط لحظة إنشائه.
- **رقم الدعم في واجهة متجر «صحة بلس»** — منشورة للعامة والرقم فيها
  `0999000000`.
