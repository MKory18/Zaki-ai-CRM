import { brandImageRoute } from '@/lib/store-brand';

/** The icon in the browser tab of the store's pages — see src/lib/store-brand.ts. */
export const { POST, DELETE } = brandImageRoute('favicon');
